#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: the string that the function checks
 * Return: int
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
