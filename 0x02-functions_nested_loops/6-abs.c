#include <stdlib.h>
#include "main.h"
/**
 * _abs - finds the absolute value of the argument
 * @n: the argument that will be checked
 *Return: Always num
 */
int _abs(int n)
{
	int num = abs(n);

	return (num);
}
